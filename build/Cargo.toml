[package]
name = "ninja-build"
version = "0.1.0"
authors = ["Nikhil Marathe <nsm.nikhil@gmail.com>"]
edition = "2018"

[dependencies]
# TODO: Move build description somewhere nicer.
ninja-desc = { path = "../desc" }
ninja-tasks = { path = "../tasks" }
ninja-metrics = { path = "../metrics" }
petgraph = "^0.5"
crossbeam = "^0.7.3"
thiserror = "^1.0"
scopeguard = "^1.1"
num_cpus = "^1.0"

[dev-dependencies]
insta = "^0.16.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

